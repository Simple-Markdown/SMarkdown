package indi.midreamsheep.app.tre.ui.editor.render.code

import androidx.compose.runtime.Composable

@Composable
fun codeInputTextField(state: indi.midreamsheep.app.tre.model.editor.line.TRELineState, stateList: indi.midreamsheep.app.tre.model.editor.manager.TREStateManager) {
/*    BasicTextField(
        value = state.content.value,
        onValueChange = {
            state.content.value = it
        },
        modifier = Modifier
            .fillMaxWidth()
            .background(Color.LightGray)
            .border(1.dp, Color.DarkGray)

    )*/
}