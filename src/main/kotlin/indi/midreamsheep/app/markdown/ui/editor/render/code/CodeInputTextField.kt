package indi.midreamsheep.app.markdown.ui.editor.render.code

import androidx.compose.runtime.Composable
import indi.midreamsheep.app.markdown.model.editor.line.TRELineState
import indi.midreamsheep.app.markdown.model.editor.manager.TREStateManager

@Composable
fun codeInputTextField(state: indi.midreamsheep.app.markdown.model.editor.line.TRELineState, stateList: indi.midreamsheep.app.markdown.model.editor.manager.TREStateManager) {
/*    BasicTextField(
        value = state.content.value,
        onValueChange = {
            state.content.value = it
        },
        modifier = Modifier
            .fillMaxWidth()
            .background(Color.LightGray)
            .border(1.dp, Color.DarkGray)

    )*/
}